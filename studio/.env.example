SANITY_STUDIO_PROJECT_ID=your-project-id
SANITY_STUDIO_DATASET=production
# Optional: fixes the hostname used by `sanity deploy` (omit if the hostname is already registered).
SANITY_STUDIO_HOSTNAME=ouoops-admin
